# URL do backend em produção (Render). O build do Vite embute esse valor.
# Se o backend usar context-path diferente, ajuste aqui ou defina VITE_API_URL no painel do Render.
VITE_API_URL=https://convivium-back.onrender.com/convivium/api/v1
